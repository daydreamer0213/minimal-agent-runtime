# 开发中的真实问题记录

本文件仅记录开发期间实际发生、并已在项目任务中确认的事项；不把假设、教程示例或未发生的故障写成项目历史。

## 1. 初始目录为空且不是 Git 仓库

项目开始时目录为空，且尚未初始化为 Git 仓库。后续先建立项目文件与版本控制基础，才有可追溯的实现和提交记录。

## 2. Git 缺少作者身份

提交时 Git 没有作者身份。为仅在本仓库完成可追溯提交，采用仓库本地身份 `Codex <codex@local>`，没有修改全局 Git 身份。

## 3. DeepSeek thinking 工具调用需要带回 `reasoning_content`

DeepSeek 在 thinking 模式下发起工具调用后，后续请求必须带回同一条 assistant 消息的 `reasoning_content`。运行时因此保存带工具调用的 reasoning，并在该消息仍在 context 中时传回模型。

## 4. 工具结果出错不能令进程崩溃

工具名、参数或执行都可能出错。错误结果需要作为可读的工具结果返回给模型，使模型能够修正参数、改用工具或向用户解释；不能让整个 Agent 进程直接崩溃。

## 5. 压缩必须以完整 turn 为边界

一轮对话中的 assistant 工具调用和对应工具结果相互关联。context 压缩必须按完整用户 turn 选择旧消息，不能把调用与结果拆开；同时保留最近两个完整用户轮次。

## 6. 初次实际 API 验证被网络故障中断

初次连接实际 API 的验证因网络故障被中断，网络恢复后再继续并完成了验证。这次中断不改变离线单元测试不访问网络的原则。

## 7. 为初学者保留本地忽略的 `.env`

在本地忽略的 `.env` 中保留真实密钥的配置方式，让初学者不必每次录屏或打开新的 PowerShell 窗口时重复输入环境变量命令。该文件保持被 Git 忽略，且不展示其内容。

## 8. GBK 窗口不能直接输出含 emoji 的中文周报

一次实际的中文周报回答含有 emoji，在严格 GBK 的 Windows 控制台崩溃。因此调整 CLI 输出边界：保留可支持的文本，仅用替代符号处理控制台不支持的符号。
